/**
 * Настройки Доса, которые влияют на счёт за Claude API.
 * Сами лимиты сообщений лежат не здесь, а в таблице app_settings в базе —
 * их можно менять прямо в Supabase, без пересборки сайта.
 */

/** Модель. Записана в CLAUDE.md; менять только после обсуждения. */
export const DOS_MODEL = "claude-sonnet-4-6";

/**
 * Сколько последних сообщений отдаём модели.
 * Без этого каждое новое сообщение тащит за собой всю переписку,
 * и цена растёт как снежный ком.
 */
export const DOS_HISTORY_LIMIT = 12;

/**
 * Сколько помнит Дос у тех, кто купил Plus. Это и есть «Дос помнит прошлые
 * разговоры» из списка Plus — обещание должно быть настоящим.
 * Платные сообщения дороже (истории больше), но их и оплачивают.
 */
export const DOS_HISTORY_LIMIT_PLUS = 40;

/** Потолок длины ответа. Дос отвечает 2–5 предложениями — этого хватает. */
export const DOS_MAX_TOKENS = 700;

/** Максимальная длина сообщения человека. */
export const DOS_MAX_INPUT = 2000;

/**
 * Примерная цена одного сообщения Досу в тенге — для прикидки расхода в админке.
 * Считана для текущей модели с кэшированием промпта; настоящую цифру смотри
 * в консоли Anthropic → Usage. Меняешь модель — поменяй и это число.
 */
export const DOS_COST_PER_MESSAGE = 3.26;
