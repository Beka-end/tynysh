/**
 * Вход по номеру телефона выключен: настоящие SMS не подключены, а тестовые
 * номера работают только у владельца. Обычный человек видит только вход по почте.
 *
 * Когда подключим SMS-провайдера — поставь здесь true, и вкладка вернётся всем.
 * Пока телефон доступен по служебному адресу /login?phone=1 — чтобы владелец
 * мог зайти в тестовые аккаунты, заведённые по номеру.
 */
export const PHONE_LOGIN_ENABLED = false;

/**
 * Вход по паролю ВЫКЛЮЧЕН по решению владельца.
 *
 * Пароль без подтверждения почты давал две дыры: человек мог зарегистрироваться
 * на чужой адрес, а забывший пароль оставался запертым снаружи — восстановить
 * его без рабочей отправки писем нечем. Поэтому вернулись к коду из письма:
 * он сам по себе подтверждает, что почта настоящая и принадлежит человеку.
 *
 * Код входа по паролю не удалён, а оставлен под этим выключателем: когда
 * заработает SMTP и подтверждение почты, связка «пароль + подтверждение»
 * станет и удобной, и безопасной — тогда достаточно поставить здесь true.
 */
export const PASSWORD_LOGIN_ENABLED = false;
